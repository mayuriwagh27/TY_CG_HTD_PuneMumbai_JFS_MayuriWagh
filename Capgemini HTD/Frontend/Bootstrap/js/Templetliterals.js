
//Templet literal
var myString = `Similarly, create split button dropdowns with virtually the same markup as single button 
dropdowns, but with the addition of .dropdown-toggle-split for proper spacing around the dropdown caret.`

console.log(myString);

//Embedded experssion
var name = 'Mayuri';
var role = 'CR';
var batch = 'Mumbai';

console.log(`I am ${name} and I am the ${role} of ${batch} batch`);

//Mathametic expression
console.log(`The sum of 10 and 20 is ${10+20}`);